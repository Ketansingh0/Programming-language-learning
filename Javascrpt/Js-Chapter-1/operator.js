// Arithmetic Operators
let a = 5;
let b = 2;

console.log("a = ", a, "& b =", b);
console.log("a + b = ", a + b);
console.log("a - b = ", a - b);
console.log("a * b = ", a * b);
console.log("a / b = ", a / b);

// Modulus Operator(%) - gives remainder
console.log("a % b = ", a % b);

// Exponention(**)
// Increment(++) --> preIncrement(++a) --> postIncrement(a++)
// Decrement(--) --> preIncrement(--a) --> postIncrement(a--)
// Assignment Operators(+=, -=, %=, /=, *=)
// Comparison Operators(==, ===, !=, !==, >, >=, <, <=)
let c = 5;
let d = "5"; // string --> number then compare with c thats why we get true.
console.log("c == d", c == d);
console.log("c === d", c === d); // It will strictly checks both value and datatype hence will give false for c and d.

// Logical Operator(&&, ||, !)