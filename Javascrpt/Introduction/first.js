var name = "Ketan Singh";      // Variable can be re-declared and updated. A global scope variable.
var name = "Adarsh Singh";    
let age = 22;                  // Variabel cannot be re-declared but can be updated. A block scope variable.
age = 20;
const a = "Hello world!!";     // Variable cannot be re-declared or updated. A block scope variable.