// Event handling in JS

/*  node.event = () =>{
    // handle here
    }
*/

// Event Object
// It is a special object that has details aboout the event.
// All event handlers have access to the Event Object's properties and methods.
/* node.event = (e) => {
    // handle here
    }
*/

// Event Listeners
// node.addEventListener(event, callback)
// node.removeEventListener(event, callback)
// Note: the callback reference shoould be same to remove